name: zorrr_black_ai
description: "Zorrr Black AI OS - Cyberpunk Interface"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: any
  system_info_plus: ^0.0.6 # Kita pakai versi yang diminta sistem agar tidak merah lagi

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
