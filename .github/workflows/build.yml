name: zorrr_black_ai
description: "Zorrr Black AI OS - Cyberpunk Interface"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: any
  system_info_plus: any # Kita ganti jadi 'any' agar GitHub yang carikan versi paling cocok

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
